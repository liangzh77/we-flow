# 原始需求

日期：2026-05-19

## 一句话目标

希望实时获取本机微信聊天记录，并使用 AI 对聊天内容进行总结、分析和建议，进一步探索是否可以由 AI 辅助甚至代替本人发送微信消息。

## 需求背景

当前希望围绕本机微信数据构建一个 AI 工作流，让 AI 能够理解日常微信沟通内容，包括群聊和一对一聊天，并基于这些聊天记录输出有价值的分析结果。

已有技术探索中，`wx-cli` 已验证可以读取本机微信数据，具备会话、未读、历史记录、搜索等能力。下一步希望围绕真实使用需求继续判断可行性。

## 核心需求

### 1. 实时获取本机微信聊天记录

希望能够实时或准实时读取本机微信聊天记录。

覆盖范围包括：

- 群聊消息
- 一对一聊天消息
- 可能还包括未读消息、新消息、历史消息

需要进一步确认：

- 能否稳定监听新消息
- 能否区分群聊和私聊
- 能否识别发送人、时间、会话、消息类型
- 能否处理图片、文件、语音、链接等非纯文本消息
- “实时”的延迟要求是多少，例如秒级、分钟级或手动刷新即可

### 2. 使用 AI 分析聊天记录

希望 AI 能够读取聊天记录后，输出总结报告、分析结果或建议。

可能的输出包括：

- 每日/每周聊天摘要
- 群聊重点事项总结
- 需要跟进的人和事
- 重要消息提醒
- 任务、约定、待办提取
- 人际关系和沟通状态分析
- 对某个会话的上下文总结
- 回复建议
- 风险、情绪、机会点分析

需要进一步确认：

- 分析粒度是按单个会话、全部会话，还是按主题聚合
- 报告是定时生成，还是在需要时生成
- 是否需要保留长期记忆
- 哪些聊天内容不允许进入 AI 分析
- 是否需要本地化处理或脱敏

### 3. 给出建议

希望 AI 不只是总结，还能基于聊天上下文给出建议。

可能包括：

- 建议如何回复某个人
- 建议下一步跟进什么
- 建议哪些消息值得优先处理
- 建议某个群聊中哪些信息有价值
- 建议如何安排时间或任务
- 建议如何处理潜在冲突、遗漏或机会

需要进一步确认：

- 建议是只展示给用户，还是可以进入自动执行流程
- 建议需要多保守
- 是否需要给出依据，例如引用相关消息片段
- 是否允许 AI 根据历史偏好形成个性化建议

### 4. 探索 AI 是否可以代替本人发微信

希望探索 AI 能不能代替本人发送微信消息，如果技术上可行，也想试一试。

这是一个待验证能力，不应默认直接自动发送。

需要重点确认：

- 当前工具链是否支持发送微信消息
- 是否只能读取，还是也能写入/发送
- 是否需要通过微信桌面客户端自动化操作
- 是否存在被微信限制、风控或封号风险
- 是否必须由用户先确认再发送
- 是否需要记录 AI 建议内容和最终发送内容

初步安全边界：

- AI 不应在没有用户确认的情况下自动发送微信消息
- 涉及钱、合同、承诺、情感关系、工作决策等高风险内容时，应要求用户明确确认
- 应该区分“生成回复草稿”和“实际发送消息”
- 自动发送能力即使可行，也应先从低风险场景、小范围白名单会话开始试验

## 初始产品形态想法

可以先从一个本地工具或后台服务开始：

- 持续读取微信新消息
- 将消息结构化存储
- 对指定会话或时间段生成总结
- 输出报告或建议
- 后续再增加回复草稿
- 最后再探索发送能力

第一阶段不一定需要复杂界面，可以先通过 CLI、Markdown 报告或本地网页验证价值。

## 隐私与安全要求

微信聊天记录属于高度敏感数据。

需要注意：

- 不把聊天原文提交到代码仓库
- 不把密钥、微信配置、数据库路径提交到仓库
- 不在普通日志里打印完整聊天记录
- 在发送给 AI 前考虑脱敏或筛选
- 明确哪些会话允许被分析
- 明确是否允许上传到云端模型
- 保留删除和关闭功能
- 对自动发送消息设置强确认机制

## 当前不确定点

- `wx-cli` 是否能稳定做到实时新消息监听
- 能否可靠读取群聊成员和消息发送者
- 能否处理图片、语音、文件等非文本消息
- 能否通过现有工具发送微信消息
- 如果需要发送，使用 CLI、数据库写入、微信客户端自动化还是其他方式
- 微信客户端升级后兼容性如何
- 长期运行是否稳定
- 隐私数据如何存储、加密和清理

## 下一步探索方向

1. 验证 `wx-cli new-messages` 或 daemon 是否能准实时读取新消息。
2. 验证群聊和私聊消息的数据结构差异。
3. 验证消息字段是否包含会话、发送人、时间、消息类型、内容。
4. 设计一个最小 AI 总结流程：读取最近消息，生成 Markdown 摘要。
5. 设计隐私边界：白名单会话、脱敏、日志策略。
6. 调研或实验微信发送能力，但先只做“生成回复草稿”，不自动发送。

## 原始表述摘录

> 希望实时获取本机的微信的聊天记录，包括群聊的，包括一对一聊天的。然后希望通过 AI 分析这些聊天记录，给我总结报告或者分析结果，或者给出建议。我不知道能不能代替我发微信，如果能的话，也想试一试。
